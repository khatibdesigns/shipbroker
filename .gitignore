node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# local env + secrets
.env
.env*.local
expo-env.d.ts

# native Firebase config (contains project keys)
google-services.json
GoogleService-Info.plist

# design handoff / store-asset staging (large; not app source)
/ShipBroker/

# Apple ASC API keys / signing secrets
*.p8

# native build output (regenerated by expo prebuild)
/ios/
/android/
/build/
